<!--
<p>
  <b>Title:</b>
  <%= @ccategory.title %>
</p>

<p>
  <b>Ptitle:</b>
  <%= @ccategory.ptitle %>
</p>


<%= link_to 'Edit', edit_ccategory_path(@ccategory) %> |
<%= link_to 'Back', ccategories_path %>
-->
<h1><%= link_to @pcategory.title, @pcategory %> &gt; <%= @ccategory.title %></h1>

<% @products.each do |product| %>
    <div class="entry">
      <%= link_to(image_tag(product.image_url), product) %>
      <h3><%= link_to product.title, product %></h3>
      <%= truncate(strip_tags(product.description), :length => 400) %>
      <div class="price_line">
        <span class="price"><%= number_to_currency(product.price) %></span>
        <%= button_to t('.add_html'), line_items_path(:product_id => product), 
        :remote => true, :class => 'btn' %>
      </div>
    </div>
<% end %>

<%= link_to t('.back'), request.referer %>
