<div style="padding-left:10px">
	<% (Pcategory.all).each do |pcategory| %>
	<dl>
		<dt><%= link_to pcategory.title,pcategory %></dt>
		<% pcategory.getchildcategories.each do |ccategory| %>
		    <dd><%= link_to ccategory.title,ccategory %></dd>
		<% end %>
	</dl>
	<% end %>
</div>

