<%= form_for([@product, @product.comments.build]) do |f| %>
  <div class="field">
	<br />
    <%= f.text_area :body, :rows => 5 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>